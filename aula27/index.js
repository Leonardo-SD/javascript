//   ?:

const ponto = 1222;

const nivel = ponto >= 1000 ? 'Usuário VIP' : 'Usuário normal';
console.log(nivel);

/*

if (ponto >= 1000) {
    console.log('Usuário VIP');
} else {
    console.log('Usuário normal');
}

*/